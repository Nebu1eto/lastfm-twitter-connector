export interface User {
  country: string;
  age: number;
  playcount: number;
  subscriber: number;
  realname: string;
  playlists: number;
  bootstrap: number;
  imageUrl?: string; // I'll take xlarge only
  registeredAt: Date;
  url: string;
  gender: string;
  name: string;
}
